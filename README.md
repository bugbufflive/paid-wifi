# 付费WiFi系统

一套完整的付费WiFi解决方案：VPS 服务端 + Cloudflare Worker + 路由器端。

## 快速开始

### 1. 部署服务端（VPS）

```bash
git clone https://github.com/your-username/paid-wifi.git
cd paid-wifi
sudo bash deploy/setup-vps.sh